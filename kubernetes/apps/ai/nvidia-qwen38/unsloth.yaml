apiVersion: apps/v1
kind: Deployment
metadata:
  name: nvidia-qwen38
  namespace: ai
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: nvidia-qwen38
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nvidia-qwen38
    spec:
      nodeSelector:
        ai.cisien.com/vendor: nvidia
      tolerations:
        - key: ai.cisien.com/gpu
          operator: Equal
          value: "true"
          effect: NoSchedule
      containers:
        - name: llama-server
          image: ghcr.io/ggml-org/llama.cpp:server-cuda
          args:
            - --host
            - 0.0.0.0
            - -hf
            - unsloth/Qwen3.8-27B-GGUF:UD-Q6_K_XL
            - --parallel
            - "1"
            - --jinja
            - --image-min-tokens
            - "1024"
            - --ctx-size
            - "262144"
            - --fit
            - "off"
            - --no-mmap
            - --temp
            - "1.0"
            - --min-p
            - "0.0"
            - --top-p
            - "0.95"
            - --top-k
            - "20"
            - --repeat-penalty
            - "1.0"
            - --presence-penalty
            - "0.0"
            - --reasoning
            - "on"
            - --reasoning-preserve
            - --reasoning-format
            - deepseek
            - --chat-template-kwargs
            - '{"preserve_thinking":true}'
            - --alias
            - incompetent-robot
            - --threads
            - "18"
            - --flash-attn
            - "on"
            - --cache-type-k
            - q8_0
            - --cache-type-v
            - q8_0
            - --metrics
            - --cache-ram
            - "32768"
            - --spec-type
            - draft-mtp
            - --spec-draft-n-max
            - "2"
          env:
            - name: LLAMA_CACHE
              value: /models
          ports:
            - name: http
              containerPort: 8080
          resources:
            requests:
              cpu: "18"
              memory: 40Gi
              nvidia.com/gpu: 2
            limits:
              cpu: "18"
              memory: 40Gi
              nvidia.com/gpu: 2
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 180
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 20
          volumeMounts:
            - name: models
              mountPath: /models
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: nvidia-model-cache
---
apiVersion: v1
kind: Service
metadata:
  name: nvidia-qwen38
  namespace: ai
  labels:
    metrics: llama-cpp
spec:
  selector:
    app.kubernetes.io/name: nvidia-qwen38
  ports:
    - name: http
      port: 8080
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: amd-qwen38
  namespace: ai
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: amd-qwen38
  template:
    metadata:
      labels:
        app.kubernetes.io/name: amd-qwen38
    spec:
      nodeSelector:
        ai.cisien.com/vendor: amd
      tolerations:
        - key: ai.cisien.com/gpu
          operator: Equal
          value: "true"
          effect: NoSchedule
      resourceClaims:
        - name: amd-gpu
          resourceClaimName: shared-amd-gpu
      containers:
        - name: llama-server
          image: ghcr.io/ggml-org/llama.cpp:server-rocm
          args:
            - --host
            - 0.0.0.0
            - -hf
            - unsloth/Qwen3.8-27B-GGUF:UD-Q8_K_XL
            - --port
            - "8080"
            - --alias
            - robot
            - --jinja
            - --n-gpu-layers
            - "999"
            - --flash-attn
            - "on"
            - --image-min-tokens
            - "1024"
            - --ctx-size
            - "1048576"
            - --parallel
            - "4"
            - --threads
            - "16"
            - --cache-type-k
            - f16
            - --cache-type-v
            - f16
            - --temp
            - "1.0"
            - --min-p
            - "0.0"
            - --top-p
            - "0.95"
            - --top-k
            - "20"
            - --repeat-penalty
            - "1.0"
            - --presence-penalty
            - "0.0"
            - --reasoning
            - "on"
            - --reasoning-preserve
            - --reasoning-format
            - deepseek
            - --chat-template-kwargs
            - '{"preserve_thinking":true}'
            - --spec-type
            - draft-mtp
            - --spec-draft-n-max
            - "2"
            - --metrics
            - --no-mmap
          env:
            - name: LLAMA_CACHE
              value: /models
            - name: HIP_VISIBLE_DEVICES
              value: "0"
            - name: ROCR_VISIBLE_DEVICES
              value: "0"
            - name: HSA_OVERRIDE_GFX_VERSION
              value: "11.5.1"
            - name: GGML_HIP_ENABLE_UNIFIED_MEMORY
              value: "1"
          ports:
            - name: http
              containerPort: 8080
          resources:
            requests:
              cpu: "16"
              memory: 64Gi
            limits:
              cpu: "16"
              memory: 64Gi
            claims:
              - name: amd-gpu
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 180
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 20
          volumeMounts:
            - name: models
              mountPath: /models
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: amd-model-cache
---
apiVersion: v1
kind: Service
metadata:
  name: amd-qwen38
  namespace: ai
  labels:
    metrics: llama-cpp
spec:
  selector:
    app.kubernetes.io/name: amd-qwen38
  ports:
    - name: http
      port: 8080
      targetPort: http
