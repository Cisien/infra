apiVersion: resource.k8s.io/v1
kind: ResourceClaim
metadata:
  name: shared-amd-gpu
  namespace: ai
spec:
  devices:
    requests:
      - name: gpu
        exactly:
          deviceClassName: gpu.amd.com
          allocationMode: ExactCount
          count: 1
