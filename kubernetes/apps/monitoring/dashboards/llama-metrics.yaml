apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: llama-cpp
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - ai
  selector:
    matchLabels:
      metrics: llama-cpp
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s